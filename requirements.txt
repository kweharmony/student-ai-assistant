
# DeepSeek API (через VseLLM провайдер, OpenAI-compatible)
openai>=1.0.0

# Веб-фреймворк
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Обработка данных
pydantic==2.9.0
python-dotenv==1.0.0

# Логирование и утилиты
python-multipart==0.0.6
aiofiles==23.2.1

# Для тестирования
pytest==7.4.0
httpx==0.25.0

# Дополнительные библиотеки
numpy>=1.26.2

# Для работы с документами (опционально)
PyPDF2==3.0.1
python-docx==0.8.11

# Для продакшена
gunicorn==21.2.0

# HTTP запросы
requests>=2.31.0

# ===== ЛОКАЛЬНАЯ ТРАНСКРИБАЦИЯ =====
# Whisper от OpenAI (локальная версия)
openai-whisper>=20231117

# FFmpeg wrapper (для обработки аудио)
ffmpeg-python>=0.2.0

# Для ускорения на GPU (опционально, но рекомендуется для Windows с CUDA)
# Установка: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchaudio>=2.0.0

# Для отображения прогресса
tqdm>=4.65.0

# CORS middleware (уже включен в FastAPI)
# fastapi уже включает starlette, который предоставляет CORSMiddleware
